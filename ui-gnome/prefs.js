// Pulse extension preferences UI — scaffold placeholder.
// Real settings (timer interval, keybind, position reset) land in the
// "GNOME extension: settings/preferences" checklist section.

import {ExtensionPreferences} from 'resource:///org/gnome/Shell/Extensions/js/extensions/prefs.js';

export default class PulsePreferences extends ExtensionPreferences {
    fillPreferencesWindow(window) {
        // Placeholder — no settings yet.
    }
}
